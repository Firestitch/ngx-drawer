export const MAIN_DRAWER_DEFAULT_WIDTH = 500;
export const SIDE_DRAWER_DEFAULT_WIDTH = 200;
export const CONTENT_DRAWER_DEFAULT_WIDTH = 209;
// All the layout the side splitter costs: the divider line itself. Its grab zone and
// hover highlight are painted outside the box, so they never enter this math.
export const SIDE_RESIZE_DIVIDER_WIDTH = 1;
export const MAIN_RESIZE_ACTION_BAR_WIDTH = 45;
